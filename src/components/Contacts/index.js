import React from 'react';
import Form from './Form'

function Contacts(props) {
    return (
        <div>
            <h1>Contacts</h1>
            <Form />
        </div>
    );
}

export default Contacts;
